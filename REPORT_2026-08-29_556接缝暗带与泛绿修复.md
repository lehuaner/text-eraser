# 556「新」字去发光接缝 —— 暗带 + 泛绿 成因与修复

- 日期：2026-08-29
- 分支：`fix/556-deglow-seam-band`
- 状态：第二轮修复完成。第一轮的两个判断（外扩环豁免补偿、软混合"dead code"）被
  用户实测推翻并已回滚，本文档记录**修正后**的最终结论。
- 状态：全量回归 11/12 图逐位零差异，仅目标图 556 变化。

---

## 一、现象（用户观察，history 1787822778556 / 556.png，v2 方案默认参数）

1. 光晕区域最外圈（处理区与正常背景交界）一圈**比两侧都暗**的脏边带；
2. 暗带不均匀，**有的弧段泛绿**，与上下相邻背景（暖灰/暖褐）色相明显不同；
3. 浅色块一侧处理区比周围浅色块暗一截；暗色块一侧泛绿；
4. 用户补充规律：**非发光区域较大（文字不占满图）的图容易出此问题**；
   且第一轮修复后 668（1787980309628，原本无问题）被引入暗带。

## 二、根因（修正后）

### 根因1（主）：暖度补偿用全局标量，而背景暖度逐弧段变化

暖背景上绿度定义 `greenness = G − max(R,B)` 系统性**低估**真实绿光：
`greenness = 真实绿光 − d(R−G 背景暖度)`。comp=d_warm 补的正是这个 d——
它不是过冲，是必要补偿（第一轮误判，见"弯路"）。

真正的问题是 **d_warm 是全 zone 一个标量**（556 实测 =7），而该图背景暖度
**逐弧段在 0~10 之间变化**（左侧橄榄 d≈0~1、右侧暖褐 d≈9~10、顶部 ≈7）：

- 橄榄弧：真实绿光 = greenness + 1，却按 +7 减 → **过减 6 → 暗带**；
- 暖褐弧：真实绿光 = greenness + 9，只按 +7 减 → **欠减 2 → 泛绿**；
- 晕尾（zone 外）：greenness≈0 完全不可见（=真实残晕 d 左右），从不被处理
  → 边界台阶。

这同时解释"暗带不均匀、有的弧段泛绿"与"非发光区域大（弧段多）的图更明显"。

### 根因2：B 场色相错（泛绿的另一半）

填充光晕区的测地背景场 B：R−G 中位 +3、G−B 中位 +12；真实背景 +10/+6
（橄榄 vs 暖褐）。旧的全局 G−shift 单标量只管 R−G 中位、修不齐 G−B 轴和逐弧偏差。

### 根因3（第一轮误判的回滚说明）：软混合不是 dead code，是验证过的行为

代码里「绿意 5→25 软混合」在 fb 内数学上是恒等式（out[fb] 已被上一行赋为
rebuilt），w 全程不起作用。第一轮按字面语义把它"修好"（混合基改为减绿后的
out），结果 **668 上 ε≤5 的 11515px 晕尾像素改为保留减绿原值——晕尾的真实
亮度（绿度低≠不亮）整圈残留，形成肉眼暗带**（用户实测确认）。
恒等混合的实际语义 =「fb 全量走 B 场重建压平（经 chroma-keep 调制）」，
这正是各图验证过无暗带的行为。已回滚，并在代码处留注释防止再犯。

### 伴随结构（非缺陷，未改）

protect2 亮环：保护圈内只减绿的残晕（保文字笔画的代价），随文字留给去字阶段。

## 三、修复（全部在 `core/text_select.py`）

| # | 修复 | 效果 |
|---|---|---|
| 1 | **减绿量改局部暖度场**：`glow = max(0, D_rg − (R−G))`，D_rg 为测地传播的局部背景暖度场；白字笔画仍不补偿；d_warm≈0 的中性背景图不走此分支（逐位保持历史行为）。对真背景像素（像素暖度=背景暖度）**自动归零**，无需任何空间豁免 | 橄榄弧不再过减、暖弧不再欠减、晕尾被正确删除 |
| 2 | **晕尾外扩**：暖背景分支把减绿区 m_zone 再外扩 14px——公式对真背景自动归零，外扩安全，把 zone 边界外 δG≈3~8 的不可见晕尾删干净 | 边界台阶消除 |
| 3 | **B 场逐像素双轴对齐**（保留第一轮）：以 B 场 R 为锚，`G=R−(R−G)_bg, B=G−(G−B)_bg`；色度参考环带从 2~14px 改为 **10~26px**——贴边环带仍在晕尾里（实测近环 R−G 中位 7 vs 远环 10），会把偏绿参考传满填充区 | 填充色相贴合每弧段背景 |
| 4 | **回滚软混合改动**：恢复 HEAD 原序（硬赋值+恒等混合），代码留注释说明为何不能"修" | 668 等大 zone 图恢复逐位原样 |

## 四、回归（基线 = main 合并基 70847b2 原始代码，各图 meta 保存参数完整 erase_text）

| 图 | mask 旧→新 | deglow 差 | 结果差 | 判定 |
|---|---|---|---|---|
| 668 | 10995→10995 | **0 逐位一致** | **0 逐位一致** | ✅ 第一轮引入的暗带已随回滚消除 |
| 178 / 635 / 换装 / 座驾2×2 / needExtractAndPatch / 020459 / 020718 / deglow_635 / t | 全部不变 | **0 逐位一致** | **0 逐位一致** | ✅ |
| 556 | 5527→5527 | 7886px (均值0.67) | 10342px (均值0.89) | ✅ 目标图 |

项目标准回归 `scripts/regress_668_fix.py`（strength=1.15, zone_expand=24）：
178/556/635/668 → mask 1273/5527/1325/10995，残迹全部 0，668 白块 63/63 ✅

556 修复后量化（默认参数，zone 边界法向环带）：
- 亮度剖面 75.2 → 79.3 → 80.4 → 81.7 → 85.4 → 86.0（内→外单调，无局部极值=无带）；
- 色相 R−G 剖面 8.8 → 9.0 → 9.2 → 9.9 → 10.7 → 10.9（单调贴背景自然梯度）；
- 残绿（绿度>2）0px；边界外 22~34px 与原图逐位相同。

## 五、弯路记录（防止重蹈）

1. 第一轮把 comp 当"过冲"在外扩环豁免 → 晕尾漏删，边界内侧正确、外侧带尾，
   色相台阶反而更扎眼（用户："暗带反而更明显"）。
2. 第一轮把恒等软混合当 bug 修 → 668 晕尾亮度残留成暗带（用户实测）。
   教训：**"死代码"先证明它无行为再动；改行为前先看它在大 zone 图上的表现。**
3. 色度参考环带取样贴边（2~14px）→ 晕尾污染参考场（近环中位 7 vs 远环 10），
   对齐残差 −1.5~−4.7；改 10~26px 后收敛。

## 六、诊断脚本（保留）

- `scripts/diag_556_seam_band.py` —— 插桩复现+逐级中间量+径向 profile（副本与真函数逐位断言）
- `scripts/diag_556_seam_quant.py` —— zone 边界法向扫描 + 残绿普查
- `scripts/diag_556_seam_arcs.py` —— 暗带成分分解 + 8 方向弧段放大
- `scripts/diag_556_seam_hue.py` —— B 场逐扇区色相 vs 同向背景
- `scripts/regress_seam_fix.py` —— 全 history 图新旧代码对比回归（基线=main 合并基）
- 图件输出：`data/_diag556seam/`（gitignored）

---

## 七、追加修复：文字区填充黑块（暗源剔除）

### 现象（用户反馈，2026-08-30）

最终去字结果里，原文字区域被填进一坨黑色内容（用户归因于本次改动）。

### 量化核查

- **黑填充是预存在问题**：HEAD 基线同一位置同样有黑块（文字框暗于55的像素
  749 vs 本轮 755，最大连通块 244 vs 324）——总量相同，本轮只是把碎块重排成
  更大的连通块，视觉上更扎眼；
- 机制：去发光后笔画上下文是深色背景（lum 74~85），**大块字洞内部锚定弱，
  patchmatch 从远处枯枝暗纹（lum 35~55，clean 中 7324px）拉黑斑填进笔画区**
  （该位置笔画白芯 lum≈159，填充后 39.9，而其周边上下文 73）。

### 修复（core/eraser.py，仅 v2 发光路径）

新增 `_dark_source_exclude`：把比「蒙版外 4px 环带亮度 25 分位 − 25」还暗的
像素从取样区剔除（并入既有 sample_exclude 机制）。参考值自校准——亮结构环绕
或深色背景图都不会误剔；仅 zone 非空（确实发生去发光）时启用。

### 效果与回归

- 556 文字框：暗像素 749→684，最大连通暗块 244→184，上簇黑块拆散；
- 回归：11/12 图逐位零差异（668/178/635 的排除集为空或未改变取样，结果不变），
  仅 556 变化；`regress_668_fix.py` 四图 mask 与基线一致、残迹 0、668 白块 63/63。
